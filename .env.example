# Optional — for Streamlit interview-question feature only (NOT used during ranking)
# GEMINI_API_KEY=your_key_here

# Optional — for LLM-based reasoning generation (NOT used during ranking)
# api_key=your_groq_api_key_here
